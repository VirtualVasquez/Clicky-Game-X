import React from "react";
import "./Header.css";

const Header = props => (
	<div className="titlebox">
		<h1>Clicky Game X</h1>
		<p>Click on a reploid to earn points, but don't click on any more than once!</p>
	</div>
);

export default Header;